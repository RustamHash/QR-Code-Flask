# QR Code Flask - Генератор QR-кодов

Простое Flask веб-приложение для генерации QR-кодов из данных Excel файлов и текстовых сообщений с возможностью создания PDF шаблонов.

## Возможности

- 📤 **Загрузка Excel файлов** (.xlsx, .xls) для генерации QR-кодов
- 📝 **Обработка текстовых сообщений** (одна строка или несколько)
- 🔲 **Автоматическая генерация QR-кодов**
- 📄 **Создание PDF файла с QR-кодами**
- 📷 **Декодирование QR-кодов из изображений**
- ⚙️ **Настройка параметров PDF** (ширина, высота, количество строк и колонок на странице)
- 💾 **Сохранение настроек пользователя** между сессиями
- 🔐 **Система авторизации** с поддержкой администраторов и обычных пользователей
- 📊 **Архив сгенерированных PDF** с фильтрацией и поиском
- 💬 **Комментарии к архивам** с возможностью редактирования

## Требования

- Python 3.10 или выше
- pip (менеджер пакетов Python)

## Установка

### 1. Создание виртуального окружения

**Windows:**
```bash
python -m venv .venv
.venv\Scripts\activate
```

**Linux/Mac:**
```bash
python -m venv .venv
source .venv/bin/activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Инициализация базы данных

```bash
python init_db.py
```

### 4. Создание пользователя

```bash
python add_user.py admin 2103 1
```

Где:
- `admin` - имя пользователя
- `2103` - пароль
- `1` - администратор (0 для обычного пользователя)

## Запуск

```bash
python app.py
```

Приложение будет доступно по адресу: `http://127.0.0.1:5000`

### Настройка через переменные окружения

Вы можете настроить приложение через переменные окружения:

```bash
# Windows
set FLASK_HOST=127.0.0.1
set FLASK_PORT=5000
set FLASK_DEBUG=True
set FLASK_SECRET_KEY=your-secret-key-here

# Linux/Mac
export FLASK_HOST=127.0.0.1
export FLASK_PORT=5000
export FLASK_DEBUG=True
export FLASK_SECRET_KEY=your-secret-key-here
```

## Использование

1. Откройте браузер и перейдите по адресу: `http://127.0.0.1:5000`
2. Войдите в систему
3. Выберите один из способов генерации:
   - **Загрузить Excel** - загрузите Excel файл с данными
   - **Ввести текст** - введите текст для генерации QR-кодов
   - **Обработать изображение** - загрузите изображение с QR-кодом для декодирования
4. Настройте параметры PDF (ширина, высота, строки, колонки)
5. Сгенерируйте PDF - файл откроется в новой вкладке для просмотра
6. Просматривайте архив всех сгенерированных PDF с возможностью фильтрации и поиска

## Формат Excel файла

- Файл должен быть в формате `.xlsx` или `.xls`
- Данные должны находиться в **первой колонке** (колонка A)
- Чтение начинается с **первой строки** (заголовок не требуется)
- Пустые ячейки будут пропущены
- Поддерживается режим "Две колонки" для парных данных

**Пример структуры Excel файла:**
```
A
1  Данные для QR-кода 1
2  Данные для QR-кода 2
3  Данные для QR-кода 3
```

## Настройки PDF

Вы можете настроить следующие параметры PDF файла:

- **Ширина страницы** (по умолчанию: 75 мм)
- **Высота страницы** (по умолчанию: 120 мм)
- **Количество строк на странице** (по умолчанию: 5)
- **Количество колонок на странице** (по умолчанию: 1)

Настройки сохраняются для каждого пользователя отдельно.

## Авторизация

### Роли пользователей

- **Администратор** - имеет доступ ко всем архивам всех пользователей
- **Обычный пользователь** - имеет доступ только к своим архивам

### Управление пользователями

Добавление нового пользователя:

```bash
python add_user.py username password 0
```

Для администратора:

```bash
python add_user.py username password 1
```

## Структура проекта

```
Qr_Code_Flask/
├── app.py                 # Основное Flask приложение
├── config.py             # Конфигурация
├── models.py             # Модели БД
├── init_db.py            # Инициализация БД
├── add_user.py           # Добавление пользователей
├── services/              # Бизнес-логика
│   ├── qr_service.py     # Генерация QR-кодов
│   ├── qr_decode_service.py  # Декодирование QR
│   ├── excel_service.py  # Обработка Excel
│   ├── text_service.py   # Обработка текста
│   └── pdf_service.py   # Создание PDF
├── templates/            # HTML шаблоны
├── static/               # Статические файлы
├── uploads/              # Загруженные файлы
├── database.db           # SQLite база данных
├── requirements.txt      # Зависимости
└── README.md            # Документация
```

## Зависимости

Основные зависимости:

- `flask>=3.0.0` - Веб-фреймворк
- `flask-login>=0.6.3` - Авторизация
- `flask-sqlalchemy>=3.1.1` - ORM для работы с базой данных
- `sqlalchemy>=2.0.23` - ORM
- `pandas>=2.1.4` - Обработка Excel файлов
- `openpyxl>=3.1.2` - Поддержка формата .xlsx
- `xlrd>=2.0.1` - Поддержка формата .xls
- `qrcode[pil]>=7.4.2` - Генерация QR-кодов
- `Pillow>=10.1.0` - Обработка изображений
- `pyzbar>=0.1.9` - Декодирование QR-кодов
- `fpdf2>=2.7.6` - Создание PDF файлов

## Разрешение проблем

### Flask не запускается

- Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
- Убедитесь, что виртуальное окружение активировано
- Проверьте, что база данных инициализирована: `python init_db.py`

### Ошибки базы данных

- Убедитесь, что база данных инициализирована: `python init_db.py`
- Проверьте права доступа к файлу базы данных

### Файлы не обрабатываются

- Убедитесь, что файл имеет расширение `.xlsx` или `.xls`
- Проверьте, что данные находятся в первой колонке
- Проверьте логи для деталей ошибки

## Лицензия

Этот проект предоставляется "как есть" без каких-либо гарантий.

